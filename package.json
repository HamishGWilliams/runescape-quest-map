{
  "name": "runescape-quest-map",
  "version": "0.1.0",
  "private": true,
  "description": "Custom RuneScape 3 map workspace for quest-cape route planning.",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "build:docs": "vite build --outDir docs && node scripts/inline-docs-build.mjs",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "leaflet": "^1.9.4",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "vite": "^5.4.19"
  }
}
